# -*- mode: c; c-basic-offset: 4; indent-tabs-mode: nil; -*-
# vim:expandtab:shiftwidth=4:tabstop=4:

#-----------------------------------------------------
# send alert in accordance to the input filename
#-----------------------------------------------------

%include "common.conf"

EntryProcessor
{
	Alert Alert_OST { type == "file" and ost_index == 1 }
}

