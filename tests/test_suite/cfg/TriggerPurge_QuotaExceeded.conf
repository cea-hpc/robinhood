%include "common.conf"

purge_trigger
{
    trigger_on = global_usage;
    high_threshold_pct = $RBH_HIGH_THR;
    low_threshold_pct = $RBH_LOW_THR;
    check_interval = 5min;
}

purge_rules { policy default { condition { last_mod >= 0 } } }
