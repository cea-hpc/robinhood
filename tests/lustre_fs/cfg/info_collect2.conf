%include "common.conf"

EntryProcessor
{
	match_classes = TRUE;
}

FS_Scan {
	ignore { type == directory and name == ".lustre" }
}

Migration_policy
{
	Policy default
	{
		condition { last_mod > 1h }
	}
}

Purge_policy
{
	Policy default
	{
		condition { last_access > 1h }
	}
}
