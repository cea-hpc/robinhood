# -*- mode: c; c-basic-offset: 4; indent-tabs-mode: nil; -*-
# vim:expandtab:shiftwidth=4:tabstop=4:

General
{
    fs_path = $RH_ROOT;
    fs_type = $FS_TYPE;
	strict_posix_times = $RH_TEST_STRICT_POSIX_TIMES;
}

ListManager
{
    MySQL
    {
        server = "localhost";
        db = $RH_DB;
        user = "robinhood";
        password = "robinhood";
    }
}
