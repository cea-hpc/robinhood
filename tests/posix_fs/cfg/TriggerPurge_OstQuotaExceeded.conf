%include "common.conf"

Purge_Trigger
{
    trigger_on = OST_usage;
    high_watermark_pct = 75%;
    low_watermark_pct = 65%;
    check_interval = 5min;
}

purge_policies { policy default { condition { last_mod >= 0 } } }
